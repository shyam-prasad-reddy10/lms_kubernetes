import React from "react";

const PostgreSQLDelete = () => {
  return (
    <div>
      <h1>PostgreSQL: Delete</h1>
      <p>This page explains how to delete data from a table in PostgreSQL.</p>
    </div>
  );
};

export default PostgreSQLDelete;